# Author's Note:

### This is an attempt to clone the app we made using Node.js and see if we can exactly do it by just using native html, css, and javascript. We used AI for polishing and fixing remaining bugs that might be unnoticed by us.
### We also used .json files for a simple database we could use for dynamic purposes(ironic for a static web  trying to be dynamic).
### We tried to use the localStorage to simulate dynamic registration and signing in but I myself don't think we can pull that off yet so for testing purposes we just have to use the prebuilt one since we cannot use the json files to store without using python since our goal is just to use native html, css, and javascript alone without any external ways to pull things off.
